# Backend fixtures

Каталог для небольших composable fixtures:
- JSON payload samples;
- SQL/bootstrap snippets;
- sample API responses;
- seeded sync/conflict inputs.

Предпочтение — маленьким reusable fixtures, а не большим дампам среды.
