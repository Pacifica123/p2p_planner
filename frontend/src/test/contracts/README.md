# Frontend contract tests

Здесь должны жить проверки, что frontend корректно понимает payload shapes из:
- `docs/api/openapi.yaml`
- `docs/sync/schemas/*.json`

Особенно важны enum values, error payloads и sync-related shapes.
