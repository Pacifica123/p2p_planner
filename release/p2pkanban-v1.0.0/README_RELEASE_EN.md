# p2pKanban v1

One web/backend/Android version without stable, dev, or beta channels.

- Windows/ReviOS bootstrap no longer depends on a CRLF-sensitive host script.
- A stopped Docker gateway retains the node's pinned port.
- Android leaves a board immediately without awaiting sync and upgrades with `versionCode 10`.
- The built-in roadmap reflects v1 outcomes and prioritized v2 plans.

Full notes: `docs/product/v1.0.0-release-notes.md`.
