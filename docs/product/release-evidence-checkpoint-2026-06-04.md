# Контрольная точка релизных доказательств — 2026-06-04

- Целевая линия на момент прогона: `v1.0.0-beta.2`
- Запуск: `20260604_050815_release-gates`
- Команда: `python tools/devbootstrap.py release-gates --profile full-local-release`
- Итог: `Overall: ok`

## Что было подтверждено

Прогон доказал работоспособность тогдашнего web/backend пути:

- управляемая тестовая PostgreSQL;
- запуск backend и frontend;
- два последовательных backend smoke;
- сборка и тесты frontend;
- UIX-сценарий с mock API;
- UIX-сценарий с настоящим backend;
- browser smoke;
- clean-machine sandbox;
- проверки документации и release notes.

Итоговая оценка была `89/100`: сырая категория `beta_candidate`, эффективная
категория `internal_candidate` из-за ограничения
`repeatability-not-proven`.

## Что этот прогон не доказывает

После 2026-06-04 в проект были добавлены transport foundation и zero-config
Docker bootstrap. Поэтому старый пакет нельзя использовать как единственное
доказательство готовности `v1.0.0-beta.3`.

Он остаётся исторической контрольной точкой: показывает, что основной
продуктовый путь уже проходил полный профиль. Для текущего релиза нужен новый
прогон, smoke собранного bootstrap ZIP и повторяемость после исправлений.

